# Pixie — Python backend dependencies
# Install with: pip install -r requirements.txt
#
# GPU note: for NVIDIA GPU acceleration with rembg install onnxruntime-gpu
# instead of onnxruntime, then use rembg[gpu] below.

fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# Image processing
Pillow>=10.3.0
numpy>=1.26.4

# Background removal (both AI models run through rembg)
# Use rembg[gpu] instead if you have an NVIDIA GPU with CUDA
rembg[cpu]>=2.0.57

# HuggingFace model downloads (used by rembg internally)
huggingface_hub>=0.23.0

# ── Optional — only needed if you re-enable transformers-based models ──
# torch>=2.2.0
# torchvision>=0.17.0
# transformers>=4.41.0
# accelerate>=0.30.0
# opencv-python>=4.9.0.80
