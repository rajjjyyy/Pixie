# Pixie — Python backend dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: For GPU acceleration install PyTorch for CUDA first:
#   https://pytorch.org/get-started/locally/
# Then run this requirements file.

fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# AI / vision
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.41.0
accelerate>=0.30.0

# Image processing
Pillow>=10.3.0
opencv-python>=4.9.0.80
numpy>=1.26.4

# rembg (U²-Net backend) — use rembg[gpu] if you have CUDA
rembg[cpu]>=2.0.57

# Optional but recommended
huggingface_hub>=0.23.0
